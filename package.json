{
  "name": "resumate-claude",
  "version": "1.0.0",
  "private": true,
  "description": "ResuMate Android packaging project",
  "scripts": {
    "android": "cap run android",
    "sync": "cap sync android",
    "ota:upload": "npx @capgo/cli@latest bundle upload --channel=production --auto-bump patch --delta"
  },
  "dependencies": {
    "@capacitor/core": "8.5.2",
    "@capacitor/android": "8.5.2",
    "@capgo/capacitor-updater": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
